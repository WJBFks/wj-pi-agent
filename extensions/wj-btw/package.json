{
  "name": "wj-btw",
  "version": "0.2.0",
  "description": "WJ BTW — 顺带一提（by the way）：受限子进程(pi --mode rpc)后端 + 结果回填主进程；btw 卡片支持底部固定窗口 + Ctrl+O 折叠/展开 + Markdown 渲染，单一结果不同时两处，reload 不丢结果，工具/技能白名单可配",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi-package", "pi", "extension", "btw", "chat", "overlay", "whitelist"],
  "files": ["extensions"],
  "pi": {
    "extensions": ["./extensions/index.ts"]
  }
}
